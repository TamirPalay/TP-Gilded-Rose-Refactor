This is a text file showing the original GildedRose.java.
This is for reference to help me explain my changes made during the refactoring process

Refactoring Summary

Replaced magic strings with constants
Item names (Aged Brie, Backstage passes, Sulfuras) are now defined once as constants, improving readability and preventing typos.

Extracted quality update logic into helpers
Repeated logic for increasing/decreasing quality and enforcing bounds (0–50) was moved into helper methods, reducing duplication and ensuring consistency.

Reduced deep nesting without changing behavior
Existing conditional logic was simplified using helpers and constants, making the code easier to read and reason about.

Preserved all existing business rules
No behavior was changed during refactoring; all domain logic remains intact and is protected by tests.

Prepared the code for future features
The cleaner structure makes it safer and easier to add new item types (e.g., Conjured items).

Everything below here is the original file:
package com.gildedrose;

class GildedRose {
    Item[] items;

    public GildedRose(Item[] items) {
        this.items = items;
    }

    public void updateQuality() {
        for (int i = 0; i < items.length; i++) {
            if (!items[i].name.equals("Aged Brie")
                    && !items[i].name.equals("Backstage passes to a TAFKAL80ETC concert")) {
                if (items[i].quality > 0) {
                    if (!items[i].name.equals("Sulfuras, Hand of Ragnaros")) {
                        items[i].quality = items[i].quality - 1;
                    }
                }
            } else {
                if (items[i].quality < 50) {
                    items[i].quality = items[i].quality + 1;

                    if (items[i].name.equals("Backstage passes to a TAFKAL80ETC concert")) {
                        if (items[i].sellIn < 11) {
                            if (items[i].quality < 50) {
                                items[i].quality = items[i].quality + 1;
                            }
                        }

                        if (items[i].sellIn < 6) {
                            if (items[i].quality < 50) {
                                items[i].quality = items[i].quality + 1;
                            }
                        }
                    }
                }
            }

            if (!items[i].name.equals("Sulfuras, Hand of Ragnaros")) {
                items[i].sellIn = items[i].sellIn - 1;
            }

            if (items[i].sellIn < 0) {
                if (!items[i].name.equals("Aged Brie")) {
                    if (!items[i].name.equals("Backstage passes to a TAFKAL80ETC concert")) {
                        if (items[i].quality > 0) {
                            if (!items[i].name.equals("Sulfuras, Hand of Ragnaros")) {
                                items[i].quality = items[i].quality - 1;
                            }
                        }
                    } else {
                        items[i].quality = items[i].quality - items[i].quality;
                    }
                } else {
                    if (items[i].quality < 50) {
                        items[i].quality = items[i].quality + 1;
                    }
                }
            }
        }
    }
}
